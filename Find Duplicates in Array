int Solution::repeatedNumber(const vector<int> &A) {
    int slow = A[0],fast=A[0];
do{
slow=A[slow];
fast=A[A[fast]];
}while(slow!=fast);
fast=A[0];
while(fast!=slow){
fast=A[fast];
slow=A[slow];
}
return fast;
}
